1. These source files are compiled using the Makefile and executed from the Linux terminal: defRefPtStreamV3.cpp/.h, DIP_FUN.cpp/.h, and findMultiPath_FUN.cpp/.h.

2. plotPath.m is a MATLAB script used to plot the reference path and the actual tracking path.